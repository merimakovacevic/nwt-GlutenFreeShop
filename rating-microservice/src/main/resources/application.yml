spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/RatingDB
    username: root
    password: mysql
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream
