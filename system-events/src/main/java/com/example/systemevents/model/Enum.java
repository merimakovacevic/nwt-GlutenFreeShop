package com.example.systemevents.model;

public class Enum {
    public static enum Action {
        GET,
        POST,
        PUT,
        DELETE
    }

    public static enum LogType {
        INFO,
        ERROR
    }
}
